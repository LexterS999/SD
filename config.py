# -*- coding: utf-8 -*-

"""
Конфигурационный файл для скрипта парсинга и валидации профилей прокси/VPN.

Все настройки скрипта вынесены сюда для удобства изменения без редактирования кода.

ИНСТРУКЦИЯ:
1. Все параметры разделены на логические блоки.
2. Для изменения параметра достаточно отредактировать его значение.
3. Не удаляйте кавычки, скобки и другие синтаксические элементы Python.
4. После изменения параметров перезапустите скрипт.
"""


# =============================================
# НАСТРОЙКИ ПАРСИНГА И ВРЕМЕННЫХ РАМОК
# =============================================

# Максимальный возраст профилей (в днях). Профили старше этого срока будут игнорироваться.
MAX_PROFILE_AGE_DAYS = 7

# Интервал обновления файла protocols.txt (в днях). После этого срока файл полностью перезаписывается.
PROTOCOLS_FILE_UPDATE_INTERVAL_DAYS = 7

# URL тестового файла для проверки скорости загрузки (размер ~100KB).
SPEED_TEST_FILE_URL = "https://www.thinkbroadband.com/download/thinkbroadband_100k.bin"

# Минимальная скорость загрузки (в KB/s) для профиля, чтобы он считался "хорошим".
MIN_GOOD_SPEED_KBPS = 300

# Максимальное время ожидания ответа при проверке скорости (в секундах).
# Увеличьте при медленном соединении, уменьшите для ускорения проверки.
SPEED_TEST_TIMEOUT_SEC = 10

# Размер буфера при загрузке тестового файла (в байтах).
# Влияет на точность измерения скорости и потребление памяти.
SPEED_TEST_BUFFER_SIZE = 8192

# Количество попыток загрузки тестового файла для усреднения результата.
# Увеличение повышает точность, но замедляет проверку.
SPEED_TEST_ATTEMPTS = 3

# Задержка между попытками проверки скорости (в секундах).
# Помогает избежать временной блокировки со стороны сервера.
SPEED_TEST_RETRY_DELAY = 1.0

# Таймаут подключения к Telegram API (в секундах).
# Увеличьте при проблемах с подключением к Telegram.
TELEGRAM_API_TIMEOUT = 30

# Максимальное количество сообщений для анализа в канале Telegram.
# Ограничивает объем данных для ускорения парсинга.
MAX_MESSAGES_PER_CHANNEL = 500

# Задержка между запросами к Telegram API (в секундах).
# Помогает избежать блокировки со стороны Telegram (rate limiting).
TELEGRAM_REQUEST_DELAY = 0.5


# =============================================
# НАСТРОЙКИ ДЕДУПЛИКАЦИИ И ФИЛЬТРАЦИИ
# =============================================

# Разрешенные протоколы (vless, trojan, tuic, hy2).
ALLOWED_PROTOCOLS = ["vless", "trojan", "tuic", "hy2"]

# Запрещенные домены/IP: профили с этими адресами будут удалены.
BANNED_DOMAINS = [
    "localhost", "127.0.0.1", "::1", "0.0.0.0",
]

# Разрешены только IPv4 адреса. Профили с доменами или IPv6 будут удалены.
ALLOW_ONLY_IPV4 = True

# Максимальное количество уникальных профилей для сохранения (0 = без ограничений).
MAX_UNIQUE_PROFILES = 2000

# Собирать профили только из хороших каналов (quality_score >= BAD_CHANNELS_THRESHOLD).
ONLY_FROM_GOOD_CHANNELS = True

# Использовать хэширование профилей для дедупликации (md5 от канонического ключа).
USE_PROFILE_HASHING = True

# Удалять профили с одинаковым endpoint (IP + port), оставляя только лучший.
DEDUPLICATE_BY_ENDPOINT = True

# Минимальный балл качества профиля для сохранения.
MIN_PROFILE_QUALITY_SCORE = 50.0

# Минимальная скорость загрузки (KB/s) для сохранения профиля.
# Профили со скоростью ниже этого порога не будут сохранены.
MIN_PROFILE_SPEED_KBPS = 300.0

# Максимальное количество дублей одного профиля перед исключением.
# При превышении все экземпляры этого профиля удаляются.
MAX_DUPLICATE_COUNT = 3

# Учитывать параметр 'alpn' при сравнении профилей на дубликатность.
# Включение повышает точность, но может уменьшить количество уникальных профилей.
COMPARE_ALPN_IN_DEDUPLICATION = False

# Учитывать параметр 'sni' при сравнении профилей на дубликатность.
# Включение полезно при наличии разных SNI для одного сервера.
COMPARE_SNI_IN_DEDUPLICATION = True

# Порог схожести профилей для объединения (0.0 - 1.0).
# Профили со схожестью выше порога считаются дубликатами.
PROFILE_SIMILARITY_THRESHOLD = 0.95

# Исключать профили с пустыми или некорректными параметрами.
# Включение обеспечивает сохранение только полностью валидных профилей.
STRICT_VALIDATION = True

# Минимальная длина строки профиля для валидации.
# Слишком короткие строки обычно являются некорректными.
MIN_PROFILE_STRING_LENGTH = 50

# Максимальная длина строки профиля для валидации.
# Слишком длинные строки могут содержать ошибки или вредоносные данные.
MAX_PROFILE_STRING_LENGTH = 2000


# =============================================
# НАСТРОЙКИ ФОРМАТИРОВАНИЯ И ВЫВОДА
# =============================================

# Формат имени профиля в конце строки (пример: #VLESS-675q7ot).
# Ключи словаря должны соответствовать названиям протоколов.
PROFILE_NAME_PREFIX = {
    "vless": "VLESS",
    "trojan": "TROJAN",
    "tuic": "TUIC",
    "hy2": "HY2",
}

# Длина случайного суффикса в имени профиля.
# Увеличьте для большей уникальности имен, уменьшите для краткости.
PROFILE_NAME_SUFFIX_LENGTH = 7

# Кодировка для входных и выходных файлов.
# Используйте 'utf-8' для совместимости с большинством систем.
FILE_ENCODING = "utf-8"

# Разделитель между профилем и комментарием в выходном файле.
# Обычно используется пробел или табуляция.
PROFILE_COMMENT_SEPARATOR = " "

# Добавлять метку качества профиля в комментарий.
# Если True, к имени профиля добавляется quality_score.
ADD_QUALITY_TO_COMMENT = True

# Формат даты для логирования и отчетов.
# Используйте стандартные директивы strftime (%Y-%m-%d %H:%M:%S).
DATE_FORMAT = "%Y-%m-%d %H:%M:%S"

# Пути к файлам ввода/вывода.
# Можно указывать абсолютные или относительные пути.
INPUT_FILE = "input.txt"
OUTPUT_DIR = "output"
PROTOCOLS_FILE = "protocols.txt"
GOOD_TELEGRAM_FILE = "output/good_telegram_channels.txt"
BAD_TELEGRAM_FILE = "output/bad_telegram_channels.txt"
LOG_FILE = "parser.log"

# Создавать резервные копии выходных файлов перед перезаписью.
# Старые файлы сохраняются с добавлением временной метки.
CREATE_BACKUP_FILES = True

# Максимальное количество резервных копий для хранения.
# При превышении старые копии удаляются.
MAX_BACKUP_COUNT = 5

# Формат имени резервного файла (используется timestamp).
BACKUP_FILENAME_FORMAT = "{original_name}.{timestamp}.bak"


# =============================================
# НАСТРОЙКИ ЛОГИРОВАНИЯ
# =============================================

# Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL).
# DEBUG - полная отладочная информация, INFO - стандартные сообщения,
# WARNING и выше - только предупреждения и ошибки.
LOG_LEVEL = "INFO"

# Максимальный размер лог-файла (в байтах) перед ротацией.
# При достижении этого размера создается новый файл лога.
MAX_LOG_SIZE_BYTES = 1048576  # 1MB

# Количество старых логов для хранения.
# Старые логи удаляются при превышении этого количества.
LOG_BACKUP_COUNT = 3

# Формат записи лога.
# Доступные переменные: %(asctime)s, %(levelname)s, %(name)s, %(message)s.
LOG_FORMAT = "%(asctime)s - %(levelname)s - %(name)s - %(message)s"

# Включить вывод логов в консоль (stdout).
# Если False, логи записываются только в файл.
LOG_TO_CONSOLE = True

# Включить запись логов в файл.
# Если False, логи выводятся только в консоль.
LOG_TO_FILE = True

# Кодировка файла логов.
# Рекомендуется использовать 'utf-8' для поддержки кириллицы.
LOG_ENCODING = "utf-8"

# Добавлять метку времени в начале каждой строки лога.
LOG_WITH_TIMESTAMP = True

# Сжимать старые файлы логов в архив (gzip).
# Помогает экономить место на диске при большом объеме логов.
COMPRESS_OLD_LOGS = False


# =============================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# =============================================

# Поддерживаемые протоколы для парсинга.
SUPPORTED_PROTOCOLS = ['vless', 'trojan', 'tuic', 'hy2']

# Порог для разделения хороших и плохих каналов.
BAD_CHANNELS_THRESHOLD = 20.0

# Стандартные порты для бонуса в оценке профиля.
STANDARD_PORTS = ['443', '80', '8443', '2053', '2083', '2087', '2096', '8080']

# Веса для оценки профилей.
PROFILE_WEIGHTS = {
    'base': 20.0,
    'ipv4_public': 15.0,
    'standard_port': 8.0,
    'metadata_richness': 12.0,
    'channel_protocol_diversity': 10.0,
    'query_cleanliness': 10.0,
    'speed_bonus': 35.0,
}

# Веса для оценки каналов.
CHANNEL_WEIGHTS = {
    'profile_count': 0.50,
    'protocol_diversity': 0.25,
    'freshness': 0.20,
    'validity': 0.20,
}

# Регулярные выражения для извлечения профилей и каналов.
PROTOCOL_PATTERNS = {
    'vless': r'(?<![A-Za-z0-9])vless://[^\s<>"]+',
    'trojan': r'(?<![A-Za-z0-9])trojan://[^\s<>"]+',
    'tuic': r'(?<![A-Za-z0-9])tuic://[^\s<>"]+',
    'hy2': r'(?<![A-Za-z0-9])hy2://[^\s<>"]+',
}

TG_PATTERNS = [
    r'(?:https?://)?(?:t\.me|telegram\.me|telegram\.dog|telegram\.org)/([a-zA-Z0-9_]{5,32})',
    r'tg://resolve\?domain=([a-zA-Z0-9_]{5,32})',
    r'@([a-zA-Z0-9_]{5,32})',
]

# Максимальное количество одновременных подключений к Telegram API.
# Увеличение ускорит парсинг, но может привести к временной блокировке.
MAX_CONCURRENT_TELEGRAM_REQUESTS = 5

# Задержка перед повторной попыткой при ошибке подключения (в секундах).
# Используется экспоненциальное увеличение задержки при неудаче.
RETRY_DELAY_SECONDS = 2.0

# Максимальное количество повторных попыток при ошибке.
# После исчерпания попыток профиль или канал помечается как недоступный.
MAX_RETRY_ATTEMPTS = 3

# Включить режим многопоточной обработки профилей.
# Ускоряет обработку большого количества профилей.
ENABLE_MULTITHREADING = True

# Количество потоков для обработки профилей.
# Оптимальное значение зависит от количества ядер процессора.
NUM_WORKER_THREADS = 4

# Таймаут для каждого потока обработки (в секундах).
# Превышение времени приводит к прерыванию обработки профиля.
THREAD_TIMEOUT_SECONDS = 60

# Включить прогресс-бар при обработке профилей.
# Отображает текущий статус и процент выполнения в консоли.
SHOW_PROGRESS_BAR = True

# Интервал обновления прогресс-бара (в секундах).
# Меньшее значение дает более плавное отображение, но увеличивает нагрузку.
PROGRESS_BAR_UPDATE_INTERVAL = 0.5

# Игнорировать профили из черного списка каналов.
# Список каналов хранится в отдельном файле blacklist_channels.txt.
USE_CHANNEL_BLACKLIST = True

# Файл со списком заблокированных каналов (по одному в строке).
# Содержит имена каналов без символа @.
BLACKLIST_FILE = "blacklist_channels.txt"

# Автоматически создавать файл черного списка, если он отсутствует.
CREATE_BLACKLIST_IF_MISSING = True

# Минимальное количество символов в имени канала для валидации.
# Слишком короткие имена обычно являются некорректными.
MIN_CHANNEL_NAME_LENGTH = 5

# Максимальное количество символов в имени канала для валидации.
# Ограничение соответствует максимальному размеру имени в Telegram.
MAX_CHANNEL_NAME_LENGTH = 32

# Включить кэширование результатов проверки профилей.
# Ускоряет повторный запуск скрипта с теми же данными.
ENABLE_PROFILE_CACHE = True

# Файл для хранения кэша проверенных профилей.
CACHE_FILE = "profile_cache.json"

# Срок жизни кэша (в часах).
# По истечении этого срока кэш считается устаревшим и пересоздается.
CACHE_TTL_HOURS = 24

# Максимальный размер кэш-файла (в байтах).
# При превышении кэш автоматически очищается.
MAX_CACHE_SIZE_BYTES = 10485760  # 10MB

# Удалять кэш-файл после завершения работы скрипта.
# Полезно для экономии места на диске при однократном запуске.
DELETE_CACHE_ON_EXIT = False

# Включить подробный режим отладки.
# Выводит дополнительную информацию о каждом этапе обработки.
DEBUG_MODE = False

# Сохранять промежуточные результаты обработки во временные файлы.
# Полезно для отладки и анализа проблемных случаев.
SAVE_INTERMEDIATE_RESULTS = False

# Директория для временных файлов.
# Оставьте пустой для использования системной временной директории.
TEMP_DIR = ""

# Кодировка для чтения входного файла.
# Если профили содержат некорректные символы, попробуйте 'latin-1'.
INPUT_FILE_ENCODING = "utf-8"

# Пропускать строки с ошибками кодировки во входном файле.
# Если False, скрипт завершится с ошибкой при встрече некорректного символа.
SKIP_ENCODING_ERRORS = True

# Максимальное количество строк для обработки во входном файле.
# Установите 0 для обработки всех строк без ограничения.
MAX_INPUT_LINES = 0

# Начальная строка для обработки (пропустить N первых строк).
# Полезно для возобновления прерванной обработки.
SKIP_FIRST_N_LINES = 0

# Разделитель строк во входном файле.
# Обычно используется перевод строки (\n).
LINE_SEPARATOR = "\n"

# Удалять ведущие и замыкающие пробелы из каждой строки.
# Помогает очистить входные данные от случайных пробелов.
STRIP_INPUT_LINES = True

# Пропускать пустые строки во входном файле.
# Рекомендуется включить для пропуска разделителей между группами профилей.
SKIP_EMPTY_LINES = True

# Пропускать строки, начинающиеся с символа комментария (#).
# Позволяет добавлять комментарии во входной файл.
SKIP_COMMENT_LINES = True

# Символ начала комментария в строке.
COMMENT_CHAR = "#"

# Включить уведомления об ошибках в консоль.
# Даже при отключенном DEBUG_MODE ошибки будут выводиться.
SHOW_ERRORS_IN_CONSOLE = True

# Цветной вывод в консоли (требует поддержки ANSI в терминале).
# Отключите для совместимости со старыми терминалами.
COLORED_CONSOLE_OUTPUT = True

# Уровень серьезности ошибок для вывода (ERROR, WARNING, INFO).
# Определяет минимальный уровень сообщений об ошибках для отображения.
CONSOLE_ERROR_LEVEL = "ERROR"

# Логировать статистику по завершении работы.
# Включает количество обработанных профилей, успешных/неуспешных проверок.
LOG_FINAL_STATISTICS = True

# Формат вывода статистики (text, json).
# JSON удобен для последующей автоматической обработки.
STATISTICS_FORMAT = "text"

# Файл для сохранения статистики (оставьте пустым для вывода только в лог).
STATISTICS_FILE = ""

# Включить отправку статистики в Telegram (если настроено).
# Требует указания TOKEN и CHAT_ID в настройках уведомлений.
SEND_STATS_TO_TELEGRAM = False

# Токен бота Telegram для отправки уведомлений.
# Получите токен через @BotFather в Telegram.
TELEGRAM_BOT_TOKEN = ""

# ID чата или канала для отправки уведомлений.
# Можно получить через бота @userinfobot или @getmyid_bot.
TELEGRAM_CHAT_ID = ""

# Включить периодические отчеты о ходе обработки (в процентах).
# Отчеты отправляются каждые PROGRESS_REPORT_INTERVAL процентов.
SEND_PROGRESS_REPORTS = False

# Интервал отчетов о ходе обработки (в процентах от 0 до 100).
# Например, 25 означает отчеты на 25%, 50%, 75% и 100%.
PROGRESS_REPORT_INTERVAL = 25

# =============================================
# НОВЫЕ НАСТРОЙКИ (Features #8-24)
# =============================================

# Детекция типа сети: Residential vs Datacenter vs CDN
# ASN базы для классификации IP
DETECT_NETWORK_TYPE = True
KNOWN_CDN_ASN = ["13335", "20940", "394", "395", "14618", "16509", "8075", "32934"]  # Cloudflare, Akamai, Amazon, Microsoft, Meta, etc.
KNOWN_DATACENTER_ASN_PARTS = ["hosting", "datacenter", "server", "cloud", "colo"]
RESIDENTIAL_ISP_KEYWORDS = ["telecom", "broadband", "cable", "fiber", "dsl", "isp", "communications"]

# Санитаризация SNI/Host/Remark
SANITIZE_SNI_HOST = True
REMOVE_TELEGRAM_ADS_FROM_REMARK = True
SPAM_KEYWORDS = ["@", "t.me/", "telegram.me", "channel", "subscribe", "promo", "bonus", "free", "unlimited"]

# TLS/Reality Handshake валидация
TLS_HANDSHAKE_CHECK_ENABLED = True
TLS_HANDSHAKE_TIMEOUT = 5.0
REALITY_PBK_MIN_LENGTH = 32
REALITY_SID_MIN_LENGTH = 8
REALITY_SID_MAX_LENGTH = 64

# Реальный Egress HTTP тест
EGRESS_HTTP_TEST_ENABLED = True
EGRESS_TEST_URLS = ["https://httpbin.org/ip", "https://google.com"]
EGRESS_TEST_TIMEOUT = 10.0
EGRESS_TEST_TOP_N = 100  # Тестировать только топ-N профилей

# Проверка разблокировки сервисов
STREAMING_TEST_ENABLED = True
STREAMING_TEST_URLS = {
    "ChatGPT": "https://chat.openai.com",
    "YouTube": "https://www.youtube.com",
    "Netflix": "https://www.netflix.com",
    "Spotify": "https://open.spotify.com",
}
STREAMING_TEST_TIMEOUT = 8.0
STREAMING_TEST_TOP_N = 50  # Тестировать только топ-N профилей

# Фильтрация Honeypot / Заблокированных IP
HONEYPOT_FILTER_ENABLED = True
RKN_GFW_BLOCKED_SUBNETS = []  # Можно добавить списки заблокированных подсетей
HONEYPOT_IP_PATTERNS = ["0.0.0.0", "127.0.0.1", "10.", "172.16.", "192.168."]

# Измерение скорости для Топ-100 профилей
SPEED_TEST_TOP_N = 100
SPEED_TEST_ENABLED = True

# Полная сырая база кандидатов
SAVE_ALL_RAW_CANDIDATES = True
ALL_RAW_CANDIDATES_FILE = "output/all_raw_candidates.txt"

# Человекочитаемый Markdown-отчет
GENERATE_MARKDOWN_REPORT = True
MARKDOWN_REPORT_FILE = "output/SUMMARY.md"

# Полный JSON-реестр с техническими характеристиками
GENERATE_DETAILED_JSON = True
DETAILED_JSON_FILE = "output/protocols_detailed.json"

# История изменений и дельта выгрузки
TRACK_CHANGES = True
CHANGELOG_FILE = "output/changelog.json"
DIFF_ADDED_FILE = "output/diff_added.txt"

# Матрица здоровья каналов-источников
GENERATE_CHANNEL_HEALTH_MATRIX = True
CHANNEL_HEALTH_MATRIX_FILE = "output/channel_health_matrix.json"

